name: Generate Anping Tide Forecast

on:
  workflow_dispatch:

  schedule:
    # 每天台灣時間 05:00
    # GitHub 使用 UTC
    - cron: "0 21 * * *"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt

      - name: Generate Tide HTML
        env:
          CWA_API_KEY: ${{ secrets.CWA_API_KEY }}
        run: |
          python main.py

      - name: Prepare Pages Artifact
        run: |
          mkdir site
          cp anping_tide.html site/index.html

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:

    needs: build

    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
